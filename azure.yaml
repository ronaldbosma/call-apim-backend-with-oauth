# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: call-apim-backend-with-oauth
metadata:
  template: call-apim-backend-with-oauth@0.0.1
hooks:
  predown:
    - shell: pwsh
      run: ./hooks/predown-remove-app-registrations.ps1
      interactive: true
    - shell: pwsh
      run: ./hooks/predown-remove-law.ps1
      interactive: true
requiredVersions: 
  azd: ">= 1.16.0"  # azd version 1.16.0 or later is required because of the use modules without names which was introduced in Bicep 0.34.1.
